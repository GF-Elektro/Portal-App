# Plan: G&F Elektro Portal Code Refinement

## Project Overview
G&F Elektro Portal is a production-ready Electron desktop application serving as a wrapper for [portal.gfelektro.com](https://portal.gfelektro.com).

## 📁 Current Project Structure
- **Main process** (`src/main.js`): Window management, tray integration, and system events.
- **Preload script** (`src/preload.js`): Secure IPC bridge and notification overrides.
- **Build scripts** (`scripts/`): Custom DMG creation, icon generation, and background rendering.
- **Release artifacts** (`release/`): Built installers for Windows and macOS.

## ✅ Verified Strengths
- **Security**: `contextIsolation: true`, `sandbox: true`, and restricted permission handlers.
- **Cross-Platform**: Tailored tray and dock behavior for Windows and macOS.
- **UX**: Dark background loading, single instance lock, and native notification bridging.
- **Auth**: Domain-allowlisted popup handling for Google/Firebase authentication.

## 🔍 Areas for Refinement
1. **Dependency Audit**: Verify that all modules used in `scripts/` (e.g., `sharp`, `png-to-ico`) are correctly listed in `devDependencies`.
2. **Build Configuration**: Review `package.json` for redundant `extraResource` entries and ensure `asar: true` doesn't interfere with debug logs.
3. **Error Management**: Implement persistent error logging to `release/debug.log` for troubleshooting production issues.
4. **Lifecycle Enhancements**:
   - Add an auto-update check mechanism.
   - Consider a lightweight loading indicator for slower connections.
   - Implement basic telemetry opt-in/out for usage metrics.

## 📋 Refinement Tasks
- [ ] Audit `package.json` dependencies and `electron-forge` configuration.
- [ ] Validate the `AUTH_ALLOWED_DOMAINS` list against the actual production auth flow.
- [ ] Enhance main process logging to capture and persist runtime errors.
- [ ] Explore integration of `electron-updater` for auto-updates.

---
*Generated by GitHub Copilot on April 22, 2026.*
